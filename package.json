{
  "name": "totp-cli",
  "version": "1.0.0",
  "description": "A CLI app for TOTP (Time-based One-Time Password) generation with interactive Google Authenticator-style interface",
  "main": "dist/index.js",
  "bin": {
    "totp-cli": "./dist/cli.js"
  },
  "files": [
    "dist/**/*",
    "README.md",
    "LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vpetar/totp-cli.git"
  },
  "bugs": {
    "url": "https://github.com/vpetar/totp-cli/issues"
  },
  "homepage": "https://github.com/vpetar/totp-cli#readme",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/cli.js",
    "dev": "tsc && node dist/cli.js",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build",
    "test": "echo \"No tests specified yet\" && exit 0",
    "lint": "echo \"No linter configured yet\" && exit 0",
    "prepare": "npm run build"
  },
  "keywords": [
    "totp",
    "otp",
    "cli",
    "2fa",
    "authentication"
  ],
  "author": "freeze.988@gmail.com",
  "license": "MIT",
  "type": "commonjs",
  "engines": {
    "node": ">=14.0.0"
  },
  "os": [
    "darwin",
    "linux"
  ],
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/inquirer": "^9.0.8",
    "@types/node": "^24.1.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "clipboardy": "^4.0.0",
    "commander": "^14.0.0",
    "fs-extra": "^11.3.0",
    "inquirer": "^12.8.2",
    "otplib": "^12.0.1"
  }
}
