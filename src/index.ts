/**
 * TOTP CLI - Main entry point
 * 
 * This file serves as the main module export for the TOTP CLI library.
 * The actual CLI functionality is in cli.ts
 */

export * from './totpManager';
export * from './storage';

// For CLI usage, this file is not directly used
// The CLI entry point is cli.ts
